define(function () {
    'use strict';

    return function () {
        // Default styles that can be overridden by themes.
        return {
            buttonType: 'plain',
            buttonColor: 'black'
        };
    };
});
